[project]
name = "nastrajacz"
version = "1.0.0"
description = "Simple configuration manager for dotfiles and server configuration."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[dependency-groups]
dev = [
    "pyte>=0.8.2",
    "pytest>=8.0",
]
